"""Source of truth for lesson:<URL_ID> (Bonus <BONUS_NUMBER> — <SHORT_NAME>).

<Тип урока: текстовый гайд / онбординг / эфир>
Source: <откуда контент — реальный опыт, ссылка на доку, скилл>
"""

TITLE = "<Полный заголовок урока>"
DURATION = "<XX мин>"
DESCRIPTION = (
    "<2-4 предложения. Зачем этот урок, что юзер получит, как использовать "
    "(если предполагается AI-помощник — упомяни кнопку «Скопировать всё (Markdown)» вверху). "
    "Без emoji.>"
)

OUTCOME = [
    "<Конкретный результат 1 — что работает / что установлено / что понятно>",
    "<Результат 2>",
    "<Результат 3>",
    # 4-7 пунктов
]

SKILLS = [
    "<Скилл 1>", "<Скилл 2>", "<Скилл 3>",
    # 5-10 коротких тегов
]

PROMPTS = [
    # (id, label, text, featured)
    (1, "<короткий label первого промпта>",
     "<Текст промпта. Многострочные через \\n или конкатенацию строк.\\n"
     "Используй \\n (двойной слэш) когда нужен литеральный newline в тексте промпта.>",
     True),

    (2, "<label второго промпта>",
     "<Текст>",
     False),

    # Только ОДИН featured=True на урок (визуальная иерархия)
]

REPOS = [
    ("<owner/repo>", "<короткое описание репо — 5-10 слов>"),
    # Только публичные или те, к которым у юзера доступ
]

TIMECODES = [
    ("00:00", "<Раздел 1>"),
    ("XX:XX", "<Раздел 2>"),
    # Для бонусов TIMECODES не показываются на странице — но оставляй для парности.
    # Для уроков с эфиром — реальные таймкоды видео.
]

# (num, title, goal, body, command_or_None, command_title_or_None, resources_or_None)
STEPS = [
    (1,
     "<Заголовок шага: глагол + объект>",
     "<Цель: что получим в результате — 1 предложение>",
     "<Что делаем: детали, инструкции, объяснения. Можно несколько абзацев через \\n\\n. "
     "Markdown **bold** поддерживается.>",
     None,  # или "bash command\\nна нескольких строках"
     None,  # или "<Заголовок code-блока, 5 слов max>"
     None,  # или [{"label": "<текст>", "href": "<URL>", "kind": "tool"}, ...]
    ),

    (2,
     "<Шаг 2>",
     "<Цель>",
     "<Что делаем>",
     "echo 'команда здесь'\\necho 'вторая команда'",
     "<Заголовок code>",
     [{"label": "<сайт.com>", "href": "https://example.com", "kind": "tool"}],
    ),

    # ... шагов 10-20 для текстового гайда
]
